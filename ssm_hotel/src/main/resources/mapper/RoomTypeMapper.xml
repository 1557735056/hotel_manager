<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.poppy.dao.RoomTypeMapper">
    <insert id="addRoomType">
        insert into t_room_type(typeName,photo,price,liveNum,bedNum,roomNum,reservedNum,avilableNum,livedNum,status,remark)
         values(#{typename},#{photo},#{price},#{liveNum},#{bedNum},#{roomNum},#{reservedNum},#{avilableNum},#{livedNum},#{status},#{remark})
    </insert>
    <update id="updateRoomType">
        update t_room_type
        <set>
        <if test="typename!=null and typename!=''">
            typeName = #{typename},
        </if>
        <if test="photo!=null and photo!=''">
        photo = #{photo},
        </if>
        <if test="price!=null">
        price = #{price},
        </if><if test="liveNum!=null">
            liveNum = #{liveNum},
        </if>
        <if test="bedNum!=null">
            bedNum = #{bedNum},
        </if>
        <if test="roomNum!=null">
            roomNum = #{roomNum},
        </if>
        <if test="reservedNum!=null">
            reservedNum = #{reservedNum},
        </if>
        <if test="avilableNum!=null">
            avilableNum = #{avilableNum},
        </if>
        <if test="livedNum!=null">
            livedNum = #{livedNum},
        </if>
        <if test="status!=null">
                status = #{status},
        </if>
        <if test="remark!=null and remark!=''">
            remark = #{remark},
            </if>
        </set>
        where id = #{id}
    </update>
    <delete id="deleteRoomTypeById">
        delete  from t_room_type where id =#{id}
    </delete>

    <select id="getAllRoomType" resultType="com.poppy.entity.RoomType">
        select * from  t_room_type
        <where>
            <if test="typename != null and typename != ''">
                and typeName like concat('%',#{typename},'%');
            </if>
        </where>
    </select>

</mapper>