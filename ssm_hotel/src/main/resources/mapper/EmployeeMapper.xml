<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.poppy.dao.EmployeeMapper">
    <resultMap id="employee" type="com.poppy.entity.Employee">
        <id property="id" column="id"></id>
        <result property="loginName" column="login_name"/>
        <result property="loginPwd" column="login_pwd"/>
        <result property="name" column="name"/>
        <result property="sex" column="sex"/>
        <result property="deptId" column="deptId"/>
        <result property="hireDate" column="hire_date"/>
        <result property="salt" column="salt"/>
        <result property="createdBy" column="createdBy"/>
        <result property="createDate" column="modicreateDatefyBy"/>
        <result property="modifyBy" column="modifyBy"/>
        <result property="modifyDate" column="modifyDate"/>
        <result property="remark" column="remark"/>
    </resultMap>
    <insert id="addEmployee">
        insert into sys_employee(login_name,login_pwd,name,sex,deptId,hire_date,salt,createdBy,createDate,remark)
        values(#{loginName},#{loginPwd},#{name},#{sex},#{deptId},#{hireDate},#{salt},#{createdBy},#{createDate},#{remark})
    </insert>
<!--    修改用户信息-->
    <update id="updateEmployee">
        update sys_employee
        <set>
            <if test="loginPwd != null and loginPwd != ''">
                login_pwd = #{loginPwd},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="sex != null ">
                sex = #{sex},
            </if>
            <if test="deptId != null ">
                deptId = #{deptId},
            </if>
            <if test="hireDate != null ">
                hire_date = #{hireDate},
            </if>
            <if test="salt != null and salt != ''">
                salt = #{salt},
            </if>
            <if test="remark != null and remark != ''">
                remark = #{remark},
            </if>
            <if test="modifyBy != null ">
                modifyBy = #{modifyBy},
            </if>
            <if test="modifyDate != null ">
                modifyDate = #{modifyDate},
            </if>

        </set>
    </update>
<!--    删除用户-->
    <delete id="deleteById">
        delete from sys_employee where id = #{employeeId}
    </delete>
    <!-- 根据登录账号查询员工信息-->
    <select id="findEmployeeByLoginName" resultMap="employee" parameterType="String">
        select * from sys_employee where login_name = #{loginName}
    </select>

    <select id="getEmployeeByDept" resultType="int">
        select count(*) from sys_employee where deptId = #{deptId}
    </select>
    <!-- 根据角色编号查询员工数量 -->
    <select id="getEmployeeCountByRoleId" resultType="int">
        select count(1) from sys_employee e
                                 inner join sys_role_employee re on e.id = re.`eid`
        where re.`rid` = #{roleId}
    </select>
<!--   查询员工列表-->
    <select id="findEmployeeList" resultType="com.poppy.entity.Employee">
        select e.* ,d.dept_name from sys_employee e inner join
        sys_dept d on d.id = e.deptId
        <where>
            <if test="loginName != null and loginName != ''">
                and e.login_name like concat('%',#{loginName})
            </if>
            <if test="name != null and name != ''">
                and e.name like concat('%',#{name})
            </if>
            <if test="sex != null ">
                and e.sex like concat('%',#{sex})
            </if>
            <if test="deptId != null">
                and e.deptId like concat('%',#{deptId})
            </if>
            <if test="startDate != null ">
                <![CDATA[ and e.startDate like concat('%',#{startDate}) ]]>
            </if>
            <if test="endDate != null ">
                <![CDATA[ and e.endDate like concat('%',#{endDate}) ]]>
            </if>
        </where>
        order by e.id
    </select>
</mapper>